// import { useAuth } from "@/contexts/AuthContext";

// export default function AvatarBox({ onAvatarClick }) {
//   const { user } = useAuth();

//   const userInitial =
//     user?.displayName?.charAt(0)?.toUpperCase() ||
//     user?.email?.charAt(0)?.toUpperCase() ||
//     "?";

//   return (
//     <div
//       onClick={onAvatarClick}
//       className="bg-trabzonBlue border  border-red-900 text-white rounded-full w-10 h-10 flex items-center justify-center text-sm font-semibold cursor-pointer hover:scale-105 transition"
//     >
//       {userInitial}
//     </div>
//   );
// }
